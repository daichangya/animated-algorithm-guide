{
  "buildCommand": "pip3 install --upgrade pip && pip3 install -r requirements.txt && python3 -m mkdocs build",
  "outputDirectory": "site",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" },
    { "src": "/.*", "dest": "/404", "status": 404 }
  ]
}